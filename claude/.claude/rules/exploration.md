When exploring a codebase, prefer spawning multiple parallel searches with varied strategies rather than one sequential search. For example:

- One searching by API/function name
- One searching by file path patterns
- One searching by related types or constants
- One via code search tools (InternalCodeSearch, GitHub, etc.)
